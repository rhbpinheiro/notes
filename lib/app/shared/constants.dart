import 'package:flutter/material.dart';

const urlApi = '';

const LinearGradient gradient = LinearGradient(
  colors: [Color(0xFF1f5466), Color(0xFF2c8f8a)],
  begin: Alignment.topCenter,
  end: Alignment.bottomCenter,
);
